<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TeaPls.Views.AboutPage"
             xmlns:vm="clr-namespace:TeaPls.ViewModels"
             Title="{Binding Title}"
  BackgroundColor="{AppThemeBinding Dark={StaticResource BackGroundColorDark}, Light={StaticResource BackGroundColorLight}}">

    <ContentPage.BindingContext>
        <vm:AboutViewModel />
    </ContentPage.BindingContext>



    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!--<StackLayout>
            <StackLayout >
                <ContentView >
                    <Image Source="xamarin_logo.png"  />
                </ContentView>
            </StackLayout>
        </StackLayout>-->
        <ScrollView Grid.Row="1">
            <StackLayout Orientation="Vertical" Padding="30,24,30,24" Spacing="10">


                <Frame CornerRadius="12" BackgroundColor="{StaticResource GridBackGroundColor}" Padding="30,24,30,24">

                    <Label Text="Tiesitkö, että teen juonti voi vähentää ihmisen onnellisuutta jopa 100%?" TextColor="{StaticResource TextColorDark}" FontSize="16" Padding="0,0,0,0"/>
                </Frame>




                <Frame CornerRadius="12" BackgroundColor="{StaticResource GridBackGroundColor}" Padding="30,24,30,24">
  
                    <StackLayout Orientation="Vertical">
                        <Label FontSize="16" TextColor="{StaticResource TextColorDark}"  Padding="0,24,0,0" x:Name="randomTea" Text="Early Gray" />
                        <Button Text="Next recommendation" Clicked="RandomTeaButton" />
                    </StackLayout>
                </Frame>

            </StackLayout>
        </ScrollView>
    </Grid>

</ContentPage>
